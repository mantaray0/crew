---
description: Deprecated alias for /crew:learn — distill reusable patterns and decisions from finished work into the global registry.
argument-hint: "[phase/milestone, optional — defaults to recent work]"
---

# /crew:retro (alias)

This command was **renamed to `/crew:learn`**. The alias stays for backward compatibility until a future major version removes it.

**Execute the steps in [`commands/learn.md`](learn.md)** exactly as written — gather → distill → propose → apply → summarize, using the `crew-learn` skill. Pass through any `$ARGUMENTS` (the phase/milestone). There is no separate logic here: `learn.md` is the single source of truth.

When you finish, mention once in your reply that `/crew:retro` is now `/crew:learn`.
