---
"@mantaray0/crew": minor
---

Add a `monorepo` archetype (all tags), a `language.files` config option (the language crew writes project files in — `PROJECT.md`/`ROADMAP.md`/`LOG.md`/`BACKLOG.md`/`plans/` — asked at `/crew:setup` and `/crew:init`, default `en`), and a `/crew:init` choice to commit or gitignore `.planning/`. Establish the `.planning/` naming convention: UPPERCASE documents (`PROJECT.md`, `ROADMAP.md`, `LOG.md`, `BACKLOG.md`), lowercase data (`config.json`, `claims.json`) and directories (`plans/`, `sessions/`).
