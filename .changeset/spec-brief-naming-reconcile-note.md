---
"@mantaray0/crew": patch
---

Documentation/flow follow-ups for the brief-naming change: note the `_<slug>.md` brief convention in the design spec, and have `/crew:init` reconcile mode offer to rename legacy un-prefixed briefs in `plans/` to the `_<slug>.md` form.
